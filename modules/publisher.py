# modules/publisher.py
import requests
import config


def publish_video(video_path: str) -> bool:
    """Publish video to Discord via webhook."""
    print("[PUBLISHER] stub — not yet implemented")
    return True